services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: rank
      POSTGRES_PASSWORD: rank
      POSTGRES_DB: rank
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
