# clearvote — Docker environment
# Copy this to .env and fill in your values

# Host port to expose the app on
APP_PORT=3000

# Auth secret — generate with: openssl rand -base64 32
AUTH_SECRET=change-me-to-a-random-secret
